{{page-title 'TechReader'}}
{{! template-lint-disable no-action }}
<Common::Header
  @userIsLoggedIn={{this.userIsLoggedIn}}
  @onNavAction={{action 'openModal'}}
/>
<section>
  {{! list of topics available }}
</section>
{{outlet}}
<footer>
  {{! copyright }}
</footer>

{{#if this.showModal}}
  {{#if this.modalType}}
    <Ui::Modal @title='Login' @onCloseModal={{action 'closeModal'}}>
      {{!-- {{#if this.userIsLoggedIn}}
      {{else}}
        <Common::Register/>
      {{/if}} --}}
      <Common::Login/>
    </Ui::Modal>
  {{/if}}
{{/if}}